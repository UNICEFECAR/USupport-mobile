import React from "react";
import { Screen } from "#components";
import { FAQ as FAQBlock } from "#blocks";

export const FAQ = () => {
  return (
    <Screen>
      <FAQBlock />
    </Screen>
  );
};
